# Namespaces

The `exercises` namespace contains Log output, Ping-pong, and future
non-project exercises. Apply it before the application manifests:

```bash
kubectl apply -f namespaces/exercises.yaml
```
